<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Viết bài mới</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="/css/style.css">
</head>

<body>
    <%- include('header') %>
    <div class="container mt-5">
        <h2>Xin chào, <%= user.username %>!</h2>
        <p>Bạn đã đăng nhập thành công.</p>
        <p>Vai trò của bạn: <%= user.role %>
        </p>

        <% if (user.role==='admin' ) { %>
                <a href="/posts/new" class="btn btn-primary">Tạo bài viết mới</a>
                <a href="/schedules" class="btn btn-primary">Quản lý lịch trực</a>
            <% } else if (user.role==='member' ) { %>
                <a href="/schedules/new" class="btn btn-primary">Đăng ký lịch trực</a>
            <% } else { %>
                <p>Bạn là khách, vui lòng liên hệ với admin để đăng ký thành viên.</p>
        <% } %>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>

</html>